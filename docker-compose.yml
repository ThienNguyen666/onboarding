services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: onboarding
      POSTGRES_USER: onboarding
      POSTGRES_PASSWORD: onboarding
    command: >
      postgres -c shared_buffers=96MB -c max_connections=40
               -c work_mem=4MB -c maintenance_work_mem=48MB
               -c effective_cache_size=192MB
    shm_size: 128mb
    ports:
      - "5432:5432"
    volumes:
      - onboarding-pg-data:/var/lib/postgresql/data
    mem_limit: 320m
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U onboarding"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    command: redis-server --maxmemory 48mb --maxmemory-policy allkeys-lru --save ""
    sysctls:
      - net.core.somaxconn=1024
    ports:
      - "6379:6379"
    mem_limit: 80m
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    build: .
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/onboarding
      SPRING_DATA_REDIS_HOST: redis
      CONDUCTOR_SERVER_URL: ${CONDUCTOR_SERVER_URL:-https://developer.orkescloud.com/api}
      CONDUCTOR_AUTH_KEY: ${CONDUCTOR_AUTH_KEY:?Thiếu CONDUCTOR_AUTH_KEY trong file .env}
      CONDUCTOR_AUTH_SECRET: ${CONDUCTOR_AUTH_SECRET:?Thiếu CONDUCTOR_AUTH_SECRET trong file .env}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-*}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    mem_limit: 448m
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  onboarding-pg-data: